// CollectAuctionPayoutFunction.ts

// Define the structure for the collectAuctionPayout function input parameter
export interface CollectAuctionPayoutParams {
    _auctionId: number;  // Auction ID for collecting the payout
  }
  