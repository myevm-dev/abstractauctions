// CollectAuctionTokensFunction.ts

// Define the structure for the collectAuctionTokens function input parameter
export interface CollectAuctionTokensParams {
    _auctionId: number;  // Auction ID for collecting the auction tokens
  }
  